/*
 8.1

 sum(l.reverse) == sum(l)

 If all elements of the list are the same value,

 sum(l) == l * l.length

 If the length of the list is 0, sum == 0

 If elements of the list are increasing from 1: (1, 2, 3, ...)

 sum(l) = l.length * (l.length + 1) / 2

 */

/*
 8.2

 max(l.reverse) == max(l)

 If all elements of the list are the same value,

 max(l) == l.head

 If the length of the list is 0, max is undefined

 */
